name: efo_similarity
channels:
    - conda-forge
dependencies:
    - r-base>=4.0
    - r-optparse
    - r-arrow
    # BioConductor packages (ontologySimilarity, ontologyIndex) are auto-installed by the script via BiocManager
