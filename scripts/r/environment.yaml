name: efo_similarity
channels:
    - conda-forge
dependencies:
    - r-base>=4.0
    - r-optparse
    - r-arrow
    # ontologySimilarity and ontologyIndex are CRAN packages, auto-installed by the script
